{
  "name": "rwtra-e2e",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.4",
  "scripts": {
    "serve": "npm --prefix ../server install && npm --prefix ../server start",
    "test": "node ./run-e2e.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "start-server-and-test": "^2.1.3"
  },
  "postinstall": "bunx playwright install --with-deps"
}
