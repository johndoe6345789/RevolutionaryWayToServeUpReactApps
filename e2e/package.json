{
  "name": "rwtra-e2e",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.4",
  "scripts": {
    "serve": "http-server .. -p 4173 -c-1 --silent",
    "test": "bunx start-server-and-test serve http://127.0.0.1:4173 \"bunx playwright test\""
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.1.3"
  },
  "postinstall": "bunx playwright install --with-deps"
}
