describe("bootstrap/helpers/helper-registry-instance.js", () => {
  it("loads without throwing", () => {
    expect(require("../../../../bootstrap/helpers/helper-registry-instance.js")).toBeDefined();
  });
});
