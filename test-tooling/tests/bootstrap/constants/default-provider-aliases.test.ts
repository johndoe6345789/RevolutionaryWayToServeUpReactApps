describe("bootstrap/constants/default-provider-aliases.js", () => {
  it("loads without throwing", () => {
    expect(require("../../../../bootstrap/constants/default-provider-aliases.js")).toBeDefined();
  });
});
