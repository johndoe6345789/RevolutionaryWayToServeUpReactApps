describe("bootstrap/constants/client-log-endpoint.js", () => {
  it("loads without throwing", () => {
    expect(require("../../../../bootstrap/constants/client-log-endpoint.js")).toBeDefined();
  });
});
